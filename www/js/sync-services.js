angular.module('sync.services', [])

.factory('syncService', ['$q', function($q) {
 
  return {
    syncSession: function(session, imageUri) {
    	
    	console.log("sincronizando sesion: " + '');
    	
    	 
    },
  }
}])